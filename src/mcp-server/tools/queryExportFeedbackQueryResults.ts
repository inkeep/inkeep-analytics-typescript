/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { queryExportFeedbackQueryResults } from "../../funcs/queryExportFeedbackQueryResults.js";
import * as components from "../../models/components/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: components.QueryFeedbackRequestBody$inboundSchema,
};

export const tool$queryExportFeedbackQueryResults: ToolDefinition<typeof args> =
  {
    name: "query-export-feedback-query-results",
    description: `Export Feedback Query Results`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await queryExportFeedbackQueryResults(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
